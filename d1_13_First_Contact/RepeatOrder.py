# A program is data in, then data out. Implement repeat_order(order). It receives an order as text and returns that same order unchanged, whatever it is. Do not hard-code one specific order: the function must return whatever it is given.
def repeat_order(order):
    return order